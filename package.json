{
  "name": "plebeian",
  "private": "true",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "pnpm --parallel --stream -r run dev",
    "lint:prettier": "pnpm --filter \"./packages/**\" --parallel --stream -r run lint:prettier",
    "format": "pnpm --filter \"./packages/**\" --parallel --stream -r run format",
    "db:setup": "pnpm --filter './packages/database' run db:setup",
    "test": "pnpm --parallel --stream -r run test"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm lint:prettier",
    "commit-msg": "pnpm exec commitlint --config commitlint.config.js --edit ${1}"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^19.3.0",
    "@commitlint/config-conventional": "^19.2.2",
    "@plebeian/eslint": "workspace:*",
    "@plebeian/prettier": "workspace:*",
    "simple-git-hooks": "^2.11.1"
  }
}
